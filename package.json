{
  "name": "draken-info",
  "version": "1.0.0",
  "description": "draken.info — The Draken 2045 Initiative",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && echo 'Build complete. Serve dist/ with any static server.'",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "dependencies": {
    "3d-force-graph": "^1.73.3",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0",
    "three": "^0.149.0"
  },
  "devDependencies": {
    "wrangler": "^4.69.0"
  }
}
